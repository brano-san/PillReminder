# --- Сборка ---
build/
.gradle/
.cxx/
.externalNativeBuild/
/captures

# --- Секреты: НИКОГДА не коммитить ---
# Ключ подписи релиза и пароли к нему. Потеря ключа = невозможность обновить
# приложение, утечка = кто угодно подпишется вашим именем. Бэкапить отдельно!
release.jks
*.keystore
keystore.properties

# --- Локальная машина ---
local.properties
.DS_Store
Thumbs.db

# --- IDE ---
.idea/
*.iml
# .vscode оставлен в репозитории намеренно: там задачи сборки (tasks.json),
# настройки JDK и рекомендации расширений — часть окружения проекта.

# --- Разное ---
*.log
*.hprof
